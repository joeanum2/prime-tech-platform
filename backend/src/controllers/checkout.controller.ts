import type { Request, Response } from "express";
import { notImplemented } from "./_common.controller";

export function checkoutStart(req: Request, res: Response) { return notImplemented(req, res); }
